---
layout: default
short_title: Home page
---

:page-liquid:

== Overview
This is a place where I can **build**, **share** and **explain** my thoughts in a comprehensible way. Its main subject is **low level programming** and **hardware** topics (it can have exceptions).

Feel absolutely free to explore! :)

'''

== Recent posts

++++
<ul class="listing">
{% for post in site.posts limit:3 %}
<li>

  <a href="{{ post.url }}">
	<img src="{{ post.icon }}">
  </a>

  <div class="content">
    <strong>
      <a href="{{ post.url }}">{{ post.title }}</a>
    </strong>

    <div class="listing-tag">
      {% for tag in post.tags %}
        <a href="/tags/#{{ tag }}">{{ tag }}</a>
      {% endfor %}
    </div>

    <div class="listing-description">{{ post.description }}</div>
  </div>

</li>
{% endfor %}
</ul>
++++
